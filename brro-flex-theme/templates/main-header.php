<?php
/**
 * The project specific main header template, called like this:
 * 
 * <header>
 *   <?php
 *     // Load main header content if it exists
 *     $main_header = get_template_directory() . '/templates/main-header.php';
 *     if (file_exists($main_header)) {
 *         include $main_header;
 *     } else {
 *         // Default main header content
 *         wp_nav_menu([
 *             'theme_location' => 'primary',
 *             'container'      => false
 *         ]);
 *     }
 *     ?>
 * </header>
 *
 * @package Brro_Flex_Theme
 */
?>
<div class="brro-dev-note">ready for development</div>